spring:
  application:
    name: ead-authuser-service
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        serviceId: ead-config-server
        default-label: main
      username: configserver
      password: 'ead123cs'

